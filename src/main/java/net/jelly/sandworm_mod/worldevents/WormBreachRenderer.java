package net.jelly.sandworm_mod.worldevents;

// Breach particles are now spawned server-side via WormBreachWorldEvent.spawnParticles()
// and sent to clients via ServerLevel.sendParticles(). This renderer is no longer used.
public class WormBreachRenderer {
}
